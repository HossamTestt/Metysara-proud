<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">#C9A96E</item>
        <item name="colorPrimaryDark">#B8935A</item>
        <item name="colorAccent">#C9A96E</item>
        <!-- Status bar: cream background with dark icons -->
        <item name="android:statusBarColor">#FAF7F2</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <!-- Fix for black screen on text selection -->
        <item name="windowActionModeOverlay">true</item>
        <!-- Do NOT overlay WebView with status bar -->
        <item name="android:windowTranslucentStatus">false</item>
        <item name="android:statusBarColor">#FAF7F2</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="android:background">@drawable/splash</item>
        <item name="android:statusBarColor">#FAF7F2</item>
    </style>

</resources>